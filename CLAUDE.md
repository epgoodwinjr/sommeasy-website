# CLAUDE.md — Sommeasy

## What This Project Is

Sommeasy is a wine recommendation app that helps users find wines from restaurant menus matching their taste preferences. A user provides a restaurant menu (photo, PDF, URL, or pasted text), sets their taste preferences and budget, and Sommeasy recommends specific bottles from that menu — with personalized sommelier notes from The Somm.

This is the full Sommeasy web app — quiz, DNA profile, restaurant recommendation engine with LLM storytelling, wine journal, and bottle logging.

**Naming:** The Palate = the DNA profile system. The Somm = the matching/recommendation system (and its voice).

## Tech Stack

- **Framework:** Next.js 14 (App Router)
- **Auth + Database:** Supabase (Postgres with RLS, auth.users) — project `zugunlctgpytgyxftllv`; a daily Vercel cron hits `/api/keepalive` (09:00 UTC, proper server-side `createServerClient`) so the free-tier project never auto-pauses again (the March 2026 pause killed the original database). Custom SMTP (Resend) is live (auth email cap 30/hr); the browser `createClient` (`src/lib/supabase.js`) FAILS THE BUILD loudly if `NEXT_PUBLIC_SUPABASE_*` are missing — a green deploy with dead auth is impossible (verified: static `/` prerender throws)
- **Styling:** Inline styles / CSS-in-JS (no Tailwind)
- **PDF processing:** unpdf with Y-coordinate detection for line breaks
- **Claude model:** one constant — `CLAUDE_MODEL` in `src/lib/anthropicConfig.js` (default `claude-sonnet-4-6`, override with the `ANTHROPIC_MODEL` env var). Never hardcode a model ID in a route; a retired hardcoded ID silently killed prod scanning for three weeks in June 2026
- **Menu scanning:** Claude Vision via `/api/parse-wine-list` (photo/PDF/scraped text)
- **Bottle label OCR:** Claude Vision via `/api/scan-label` (tesseract.js was removed — it was a declared-but-never-called dependency)
- **Somm curation:** `/api/somm-picks` — one Claude call turning scored candidates into curated picks + notes
- **Deployment:** Vercel (auto-deploys from main)
- **Core flow:** Scan/URL/paste → structured wine data → DNA matching (with journal feedback signals) → algorithmic curated picks render instantly → The Somm re-curates with storytelling in the background

## Current State (July 2026)

- Quiz (5 steps); the DNA profile's identity is a per-user **identity strand** composed from taste content by `src/lib/identityEngine.js` (Act III — the 15 shared archetypes are retired)
- **The Reveal is live (July 29):** quiz completion AUTO-SAVES (no Save button — a signed-in user never has an unsaved completed quiz), then stages the archetype reveal and hands off to /palate ("Meet your palate →"). Post-quiz recs are ratable via the shared `WineRecList` component — the ONE implementation of Had it / Want to try / Not for me, used by both the reveal and home's Wines to Try, wired to the full rating → dnaEvolution path. **Merge semantics (updated Aug 2026 — the uncheck is honored):** a refine save writes quiz selections ∪ earned-promoted DNA (`mergeQuizWithEarnedDna`) — retaking the quiz never SILENTLY erases what rated bottles proved — but an EXPLICIT deselection is respected: an earned item that was pre-checked at refine start (in `initialRaw`) and unchecked at save leaves the profile and its promotion un-flags, points surviving so continued love re-promotes. Earned items the quiz UI couldn't display (or that promoted mid-quiz) are still preserved — the quiz never removes values it didn't render, so initial-minus-final is exactly the set of chips the user saw and turned off. Refine chips wear a ✦ ("earned by your ratings — unchecking one removes it from your palate"). `reconcileQuizPromotions` un-flags promoted accumulation rows removed from the DNA (no timeline events — user edits aren't rating evidence), and `syncQuizSelections` preserves `source='auto'` on earned rows so the ✦ survives a refine. Specific-wine names are normalized with `formatWineName` at save. The narrative regenerates from the merged palate on every quiz save; `narrative_updated_at` is left untouched so `/api/palate-narrative` re-evolves it under its usual staleness gate
- **The Front Door, Session 1 is live (July 30):** auth core rebuilt on `@supabase/ssr`. New token_hash `GET /api/auth/confirm` (cross-browser email links, `verifyOtp`) + rebuilt PKCE `GET /api/auth/callback`, both writing session cookies onto the redirect via `supabaseRoute.js` and validating `next` with `sanitizeNext` (open-redirect killed); every failure lands on `/login?error=<link_expired|exchange_failed>` — never silent. `src/middleware.js` runs the standard session refresh; palate-narrative's `setAll` no-op is fixed (refresh-token rotation loss). AuthForm is truthful: already-registered detection (`identities.length === 0`), straight-in when a session arrives, dedicated check-your-inbox state with 60s-gated resend, `?error`/`?email` landings rendered. ALL auth copy lives in `src/lib/authCopy.js` — never render a raw Supabase string. Full forgot/update-password flow (`/forgot-password`, `/update-password`, anti-enumeration copy). Google button renders only when `NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=1` (default off until the provider is enabled in Supabase). Branded token_hash email templates in `docs/email-templates/` — paste into the dashboard together with this deploy; old `ConfirmationURL` links keep working same-browser via the callback until then
- **Hardening & Watchtower, Session 4 is live (July 31) — auth overhaul COMPLETE:** `/api/fetch-menu` SSRF-guarded via `src/lib/ssrfGuard.js` (dependency-free IP classifier — private/loopback/link-local/metadata/CGNAT/ULA/multicast, IPv4 + IPv6 incl. IPv4-mapped forms; `assertPublicUrl` resolves DNS and validates EVERY address; `safeFetch` follows redirects manually and re-validates every hop with a cap — numeric-IP bypasses like `http://2130706433` are caught because getaddrinfo normalizes them through the same check), plus `checkRateLimit("fetch-menu")` before any fetch; SSRF rejections return brand copy + `[fetch-menu] blocked: <reason>` logs, never leaking the probed host. Turnstile captcha is PREPARED but dark (`src/lib/captcha.js`, `NEXT_PUBLIC_CAPTCHA_ENABLED` default off, fails open on misconfig/outage). Rate limiter documents its per-instance limitation (Redis/KV upgrade path noted). Observability: confirm/callback emit `[auth] <phase> ok` success lines (the funnel numerator) alongside the existing `[auth] <phase> failed: <reason>` lines; the weekly health-check ritual is `docs/auth-watchtower.md` (what to grep, healthy baselines, escalation signal per line). Note: the pure-auth path (signup/token/otp) hits Supabase's own rate-limited endpoints, not ours — only fetch-menu and the paid Claude routes are ours to limit
- **Polish the Brass, Session 3 is live (July 31):** best-in-class form furniture on all three auth forms via the shared `AuthField` (AuthShell.js) — visible labels, `name`/`id`, `autoComplete` (`email` / `current-password` on login / `new-password` on signup AND update-password — the iOS/1Password strong-password trigger), `inputMode`, autoFocus, password visibility toggle, "At least 8 characters." helper. minLength 8 on password-CREATING fields only (login stays length-agnostic so legacy shorter passwords still sign in — flip the dashboard policy to 8 with this deploy). Pending discipline: one `anyPending` switch disables every control, real pending copy everywhere. Errors: `role="alert"` + `aria-live="polite"` + focus moves to the error on interactive failure (never on the ?error param — autoFocus owns first paint). Logo links home; funnel cross-links are next/link. **Magic link** behind `NEXT_PUBLIC_MAGIC_LINK_ENABLED` (repo default OFF; set in gitignored .env.local for dev/e2e; Ed flips in Vercel once SMTP is live): `signInWithOtp({ shouldCreateUser: false })`, anti-enumeration by construction — the unknown-email rejection (`isOtpNoAccountError`) lands on the SAME check-inbox copy as success; resend re-invokes signInWithOtp (`inboxMode` on the shared check-inbox state)
- **Make the Reveal Land, Session 5 is live (July 30) — the canary fixes:** Ed's acceptance canary (anonymous quiz → Hide My Email signup → confirm in an in-app browser) lost the quiz because S2's stash was localStorage-only. Now the pending palate ALSO rides the account: signup attaches compact answers as `user_metadata.pending_palate` (`peekStash`+`buildMetadataPayload` — peek NEVER claims, 8KB size guard falls back to localStorage-only, any failure leaves signup untouched), and `restorePendingPalate` reads BOTH carriers (union when both — never lose either; metadata has version+shape gates but deliberately NO expiry: it's account-bound by explicit intent), clearing BOTH after a successful save (`updateUser({pending_palate: null})`, awaited, so welcome-back implies a clean record; a failed clear only re-runs a content-identical union). The fallback archetype is rewritten: "The Rising Palate" 🌱 (deferral copy — "your profile will grow richer" at the emotional payoff) is now **"The Instinctive Palate"** 🌿, a confident present-tense identity built from real data (Old/New World lean, noun-phrase anchors); NO archetype narrative may lead with deferral — enforced by archetypeVoice.test.js's source scan, so never quote banned copy even in profileEngine comments. Email templates got the protea mark (absolute prod URL) and friendly fallback-link text (raw token URL removed — spam-filter trigger; hrefs unchanged)
- **Never Lose a Palate, Session 2 is live (July 30):** an anonymous quiz stashes `{version, createdAt, answers, profile}` in localStorage (`sommeasy.pendingPalate`, 7-day expiry + version gate — `src/lib/pendingPalate.js`) at reveal time; the first authenticated load of `/` claims the stash SYNCHRONOUSLY (two-tab idempotency — a failed save re-stashes, nothing lost) and folds it in via `saveQuizProfile` (the ONE quiz-save path, extracted from page.js — refine merge semantics unchanged). Over an existing profile it saves stash ∪ existing ∪ earned (`unionQuizRaw` + refine merge with `initialRaw=null` — the anonymous quiz never displayed existing chips, so nothing counts as a deselection; NEVER clobber). Success lands the profile view with the welcome-back moment ("Welcome back. Your palate was waiting."). Anonymous reveal is a teaser: archetype hero + narrative stay (gate depth, not delight), signature-line partial read, 3 read-only recs with anonymous copy (never "rate the ones you know"), gate copy "Create your account to meet your full palate." + "Sign in — we'll fold this into your palate." Signed-out gates on /palate, /journal, /recommend pass `?next=` and AuthForm honors it end-to-end (push, emailRedirectTo, OAuth, login↔signup toggle), always through `sanitizeNext`
- **The Evidence Ledger, Act III Session 1 is live (July 30):** every tasted-and-rated bottle counts, exactly once, on every surface. /recommend's `handleRatePick` runs the full `resolveAndAccumulate` path with `previousRating` (restaurant ratings — the strongest taste signal — used to add ZERO DNA and fed null resolved_* to the somm payload); home `handleBottleSave` reads the previous rating before upserting (re-logging no longer double-counts); journal delete deletes FIRST (`.delete().select()` — reversal runs only when THIS call actually removed the row, so a failed delete + retry, or a second tab, can never double-reverse; `reverseAccumulation` takes the pre-read row). Promotion thresholds retuned to a real drinking pace in `dnaThresholds.js` (still the single copy): estate 4, varietal 6, region 10, country 14 — 2 loved bottles to an estate, 3 to a varietal. **Partial credit adopted:** confidence 60–79 accumulates at integer-capped weights (`PARTIAL_RATING_POINTS` = loved +1, liked +1, fine 0, not_for_me −1) on varietal/region/country only, NEVER estate — a fuzzy producer match must not accrue estate loyalty; full points at ≥80, nothing below 60 (`PARTIAL_CONFIDENCE_GATE`). The caps are deliberately integers rather than literal halves: `points` is an INTEGER column, and only a fixed integer points-per-rating function keeps re-rates and deletes exactly reversible — rounding fractional deltas breaks path-independence. Migration 006 adds `wine_profiles.identity` (jsonb, empty until Act III Session 2) and widens the dna_timeline event_type CHECK to include `'shifted'` (Session 3). Accepted legacy drift: rows rated BEFORE this session at 60–79 confidence, or through the old /recommend path, never accumulated — a later re-rate or delete applies a differential against points that were never added (production is ~6 profiles; revisit in the Act III prod migration, Session 5)
- **One of One, Act III Session 2 is live (July 31):** the archetype engine is retired; `composeIdentity(evidence)` in `src/lib/identityEngine.js` composes each user's identity strand — deterministic, pure, client-runnable (the anonymous teaser and pendingPalate restore regenerate it with no DB or LLM). Title = "The {Anchor} {Persona}": anchor precedence region → country → signature grape, with a clunk rule (anchor names > 14 chars or > 2 words step DOWN the precedence — "Willamette Valley" yields to "US"; grape names shorten to their first word, "Cabernet Sauvignon" → "Cabernet") and a varietal-dominance rule (one grape, no mapped places, several countries → the grape outranks the country: "The Riesling Seeker"). Persona vocabulary: 15 shape-words (Loyalist, Archivist, Curator, Purist, Cartographer, Devotee, Voyager, Classicist, Pioneer, Wayfarer, Regular, Faithful, Seeker, Instinct + the no-anchor "Instinctive Palate"), selected by ordered first-match-wins rules — total, tie-free, NON-HIERARCHICAL (tier words are banned by identityVoice's source scan, comments included — so never write one even in a comment there). The title lives on in `wine_profiles.archetype` (every surface + testid unchanged); epithet/traits/genome ride the `identity` jsonb (migration 006); `archetype_emoji` holds a neutral 🧬 and is no longer rendered anywhere (the visual signature replaces it in Session 4). Reveal, home strip, and /palate render title + epithet (`reveal-epithet` / `palate-epithet` testids; strip and palate fall back to the live `signatureLine` when identity is absent). All 7 production rows were regenerated under the new engine July 31 (before/after in `docs/palate-act-iii-s2-readout.md`) — the "Rising Palate" fossil is dead by construction ("The Bordeaux Regular" now), and NO dna_timeline events were written (a migration is not rating evidence)
- **The Living Strand, Act III Session 3 is live (July 31):** identity now recomposes at milestone moments, not just quiz saves. **Part 0 first:** country-anchored titles wear a curated adjective (`COUNTRY_TITLE_NAMES` in identityEngine — "The Italian Faithful", "The South African Curator", "The French Purist"; wine-register picks: Argentine not Argentinian, American for the US; New Zealand deliberately keeps its noun) via the existing titleName mechanism — TITLES ONLY, epithets/narrative keep the place noun; the 4 affected production rows were regenerated (archetype column only, zero timeline events, `narrative_updated_at` untouched). **The milestone hook:** `maybeRecomposeIdentity` (`src/lib/identityRecompose.js` — the hook owns ALL I/O; identityEngine stays pure) runs non-blocking after `resolveAndAccumulate`/`reverseAccumulation` on all four rating surfaces. Milestones: any promotion/demotion; every 5th rated bottle since the last recompose (baseline = LIVE rated count at last recompose, stored in `identity.milestones.ratedCountAtLastRecompose`, reset by every recompose INCLUDING quiz saves — deletes subtract progress); first-time firsts (first earned promotion, first loved bottle fully outside the DNA — defined as every band-accumulated dimension absent from the profile arrays as read after the engine run — and first demotion; durable once-per-account flags in `identity.milestones.firsts`, armed only by LIVE engine results, never by stored promoted rows or migrations). On milestone: recompose from profile arrays (quiz ∪ earned) + accumulation rows (evidence points pick anchors), live red/white recount written to red_count/white_count (the frozen rail finally moves); title-or-epithet change → CAS-guarded write (compare-and-swap on archetype + `identity->>epithet` — a two-tab race celebrates once; residual window: a crash between CAS and event insert loses the celebration, never doubles it) + `dna_timeline` 'shifted' event (dimension `'identity'` — migration 007 widened the dimension CHECK; event_type alone wasn't enough — dimension_value carries before→after JSON, display_name = the new title); unchanged strand → silent traits/genome/recount refresh. The narrative is NOT touched — the shifted event re-arms the palate-narrative staleness gate and the payload now carries the epithet (and describes shifted events as becomings, not rotations). Celebrated surfaces: shift toast after promo toasts ("🧬 Your DNA has shifted: you're now The Mosel Purist"; epithet-only shifts show the new signature), home-strip whisper ("You became …"), /palate Recently evolved ✨ entry, journal timeline row. Quiz saves stay silent by construction (no timeline writes — Suite 12 source-scans this) and preserve firsts while resetting the interval baseline. Migration 008 adds an owner-scoped DELETE policy on dna_timeline — the insert-only contract is the APP's (no product code path deletes history; UPDATE stays impossible); it exists solely so the identity-shift e2e guard can restore its exact baseline as the RLS-bound test user
- Restaurant recommendation engine: scan (Claude Vision), URL fetch, PDF extraction, paste — two-card input UX with optional occasion field (also editable in results)
- **The Somm is live:** LLM curation + 2–3 sentence pairing-first notes per pick + a list-level summary, in brand voice. Strict server-side validation; ANY failure returns `{fallback:true}` and the UI silently keeps algorithmic picks — The Somm is progressive enhancement, never load-bearing
- **DNA feedback loop is wired:** rated journal entries (loved/not_for_me) boost and suppress scores in `matchWinesAgainstDNA` and feed the somm payload
- **The Palate view (/palate) is live** (Act II, July 29): the home DNA strip is the door (archetype + signature line + evolution whisper); the view holds the palate signature (Old↔New World, Focused↔Wide-ranging, Red↔White rails + concentration phrase via `palateSignature.js`), the narrative, Recently evolved (dna_timeline) and Building now (dna_accumulation progress against the real `dnaThresholds.js` promotion thresholds — never hardcode them), founding-vs-earned ✦ marks, and the full DNA sections. `PalateView.js` is presentation-only (props in) so a teaser variant can reuse it. Quiz entry is only quiet links (`/?quiz=refine|fresh`)
- Wine Journal (/journal): Tried, Want to Try, Skipped tabs with ratings + DNA Timeline (deep-linkable via `?tab=timeline`)
- Log a Bottle: Claude Vision label extraction, saves to journal + evolves DNA (accumulation/promotion/demotion engine in `dnaEvolution.js`)
- Rate limiting: in-memory sliding window, 10 requests/hour/IP per paid route (`src/lib/rateLimit.js`); cost logging: one `{"type":"claude_usage",...}` JSON line per Claude call — Vercel logs are the cost dashboard
- Supabase tables: wine_profiles, wine_interactions, dna_accumulation, dna_timeline

### Tests (run all before shipping)

- `node src/lib/__tests__/dnaEvolution.test.js` — 74 tests, DNA evolution pipeline (inlined mirror of resolver + engine, incl. Suite 9 quiz-merge + uncheck invariants, Suite 10 partial-credit banding + the 10H geography-conflict guard (a conflict-capped resolution accumulates nothing, not even partial credit), Suite 11 surface flows: the /recommend accumulation path, the home double-count regression, and delete-retry idempotency; Suite 12 runs the REAL `maybeRecomposeIdentity` module through the alias loader against the mock DB — every-5th interval + baseline reset, shifted-only-on-actual-change with the before→after event shape, firsts fire once and never from stored promoted rows, quiz-save silence (source guard: saveQuizProfile must never mention dna_timeline), the two-tab CAS race, and the live red/white recount)
- `node src/lib/__tests__/countryAttribution.test.js` — 22 tests, country-misattribution regression suite (the Cassis→US / Gimonnet→Italy / Crozes→Portugal / Maté's-Vineyard→Italy class): mirror sections plus a real-module section that runs `resolveWine` through the alias-loader, incl. the possessive guard ("X's" never counts as a producer mention) and the explicit-geography conflict cap (producer country contradicted by the name → confidence capped at 55, below `PARTIAL_CONFIDENCE_GATE`)
- `node src/lib/__tests__/sommPicks.test.js` — 24 tests, somm payload builder + response validator (tests the real module via dynamic import)
- `node src/lib/__tests__/authFlow.test.js` — 56 tests, open-redirect kill table (`sanitizeNext`), signUp outcome branches, brand-voice error map, magic-link copy + `isOtpNoAccountError` (real modules via dynamic import)
- `node src/lib/__tests__/authRoutes.test.js` — 21 tests, the REAL `/api/auth/confirm` + `/api/auth/callback` route handlers (a `module.register` hook in `helpers/alias-loader.mjs` resolves `@/` and `next/server` for plain node; GoTrue is a scripted `globalThis.fetch`) — asserts session cookies are SET on redirects and every failure lands on `/login?error=<reason>`
- `node src/lib/__tests__/pendingPalate.test.js` — 26 tests, the anonymous-quiz stash (expiry/version/claim-idempotency/restore), the Session 5 metadata carry (peek-not-claim, compact payload + 8KB size guard, parse gates, deliberate no-expiry) + `unionQuizRaw` never-clobber table (real module via dynamic import)
- `node src/lib/__tests__/identityVoice.test.js` — 18 tests, the identity-strand guard (replaces archetypeVoice): a SOURCE SCAN of identityEngine.js for banned deferral copy ("come back", "will grow richer/get sharper", the old "Rising Palate" name) AND tier words (grand/master/expert/… — non-hierarchical personas), comments included, so never quote banned copy or tier words there; plus real-module cases — determinism (same evidence → same strand), France+Cab composes confidently with no fabricated places, minimal input gets a real identity, rich input never lands in the sparse register, the clunk rule steps down, titles/epithets/narratives never leak raw ids; plus the S3 Part 0 adjective block — country anchors wear their adjective in the title ONLY (epithets/narrative keep the noun), the wine-register picks hold (Argentine, American, the deliberate New Zealand noun), and a completeness check that every wineUnified country has a non-clunking `COUNTRY_TITLE_NAMES` entry
- `node src/lib/__tests__/ssrfGuard.test.js` — 48 tests, the SSRF block table (IPv4/IPv6/mapped, fail-closed on junk), `assertPublicUrl` protocol+resolve+validate with injected lookup, `safeFetch` per-hop revalidation + hop cap (real module)
- `node src/lib/__tests__/captcha.test.js` — 8 tests, the dark-by-default Turnstile scaffold (disabled no-op, fail-open on misconfig/outage, verified/rejected token paths via stubbed fetch)
- `npm run test:e2e` — 60 Playwright specs (plus an env-gated seeder, skipped in normal runs), running exclusively against the dedicated test account, serialized (`workers: 1` — every spec shares the one account, and the Act III guards assert exact DB state; parallel files would race each other's baselines). Fixture images are gitignored; regenerate with `python3 e2e/fixtures/generate-fixtures.py` or the suite silently collects 0 tests. Includes twelve permanent hard-fail guards — fail-if-no-picks, no-raw-internal-IDs (raw-ids.spec.ts, covers home/palate/journal), the Palate-view render guard (palate.spec.ts), the quiz-completion Reveal guard, and the refine-uncheck guard (both in quiz-completion.spec.ts; mutations are 0-point ratings cleaned via the journal UI, and the earned-✦ fixture self-heals via e2e/fixtures/test-db.ts), the signup happy-path guard (auth-flows.spec.ts — all `/auth/v1/*` mutations are Playwright-intercepted, NO real users created; since Session 5 it also asserts the signup body CARRIES `data.pending_palate` when a stash exists, and that the stash survives the carry), the three never-lose-a-palate guards (palate-handoff.spec.ts — anonymous quiz must stash at reveal time; a signed-in landing must fold the stash in with the welcome-back moment; and the Session 5 metadata guard: `pending_palate` user_metadata alone, stash-free browser, must restore the palate and be cleared on the real auth record afterwards; specs B/C use a SUBSET of the seeded answers so every merge is content-identical and the fixture never drifts), and the password-manager guard (auth-flows.spec.ts Session 3 block — every password field must keep its autoComplete: current-password on login, new-password on signup AND update-password; losing one silently kills iOS/1Password save/generate), and the evidence-ledger guard (evidence-ledger.spec.ts — rating a pick through the real /recommend UI must move dna_accumulation +2 per dimension and stamp resolved_* metadata, and deleting the row through the journal UI must reverse it exactly; guard rows are identified by source_url='text_paste', self-healed, and the baseline restored — since S3, the restore also puts back archetype/identity/red_count/white_count because the milestone hook may silently refresh them mid-run), and the identity-shift guard (identity-shift.spec.ts — a Dujac estate promotion earned through the real /recommend UI must recompose the strand: shifted timeline row (dimension 'identity', before→after JSON), shift toast, title/epithet change, and the stored strand must equal what `composeIdentity` composes from the live evidence (the spec imports the real engine); its dims (dujac/burgundy/france) deliberately never overlap evidence-ledger's; exact restore including the identity jsonb and the shifted event, via the migration-008 owner-scoped timeline DELETE policy) — keep all; outcome-tolerant specs masked a dead integration for weeks once

## Brand & Design

### Colors
- **Deep forest green** — primary
- **Burgundy** — accent
- **Sage** — secondary/supporting
- **Cream** — backgrounds and light surfaces

### Typography
- High-contrast serif typography
- The brand feels sophisticated but approachable — think "knowledgeable friend who knows wine," not "stuffy sommelier"

### Design Principles
- Clean, uncluttered layouts with generous whitespace
- Visual hierarchy should guide the eye naturally — don't rely on users reading everything
- Photography and imagery should feel warm, real, inviting (not stock-photo sterile)
- Mobile-first — most users will hit the site from phones. Form inputs stay ≥16px font (iOS auto-zoom) and tap targets ≥40px

## Voice & Tone

Sommeasy's voice is:
- **Confident but not pretentious** — we know wine, but we don't gatekeep
- **Warm and conversational** — like a friend giving you a recommendation
- **Clear and direct** — no jargon unless it genuinely helps

Bad: "Leveraging AI-powered algorithms to curate optimal wine pairings"
Good: "Tell us what you like. We'll find it on the menu."

Every error and empty state follows this voice: warm, no raw error strings, always tells the user what to do next.

## How I Work With You (Claude Code)

### Decision Authority
You have autonomy to make implementation decisions. When given a task, you should:
1. Understand the goal and the *why* behind it
2. Make the changes you think best achieve that goal
3. Explain what you did and why after the fact

You don't need to ask permission for individual code changes. Make the call, ship it, explain it.

### What to Escalate
- Architectural decisions that would be hard to reverse (new dependencies, data model changes, major refactors)
- Anything that changes the user-facing product direction (not just polish — actual feature/flow changes)
- If you're unsure whether something aligns with the brand or priorities, flag it

### Quality Bar
- Code should be clean and readable, but don't over-engineer for hypothetical future needs
- Every change should have a clear reason — no changes for the sake of changes
- Test your work. If something could break, verify it doesn't
- **Production verification must never leave derived state behind.** Anything seeded into real user data during verification must be fully reverted INCLUDING downstream artifacts — regenerated narratives, dna_accumulation points/flags, dna_timeline rows — not just the seeded rows themselves. If a full revert isn't practical, verify with the TEST_USER account instead of real data. **Ed's personal account (epgoodwin@gmail.com) is off-limits to automation** — e2e and any verification that writes data run exclusively against the dedicated test account (`epgoodwin+e2e@gmail.com`, credentials in `.env.local` as TEST_USER_EMAIL/PASSWORD; reseed its fixture with `SEED_TEST_ACCOUNT=1 npx playwright test e2e/seed-test-account.spec.ts`)
- Commit messages should be descriptive: what changed and why, not just "update files"

### Session Workflow
- When starting a session, read this file and orient yourself
- You'll typically receive a task framed as a goal, not a list of steps
- Make 3-5 meaningful changes per session when appropriate
- End each session with a summary: what you changed, why, and anything I should review closely

## Key Technical Context

- PDF text extraction uses Y-coordinate detection (transform[5]) to preserve line breaks — naive extraction merges everything into one blob and breaks the parser
- Wine name cleanup handles: dot leaders, bin numbers, broken accent characters, section headers misidentified as wine entries, US state abbreviations
- **Unified data architecture:** Single `wineUnified.json` (built by `scripts/build_quiz_data.py` from 130k WineMag reviews) powers all engines — quiz, profile, match. Contains countries, regions, producers, varietals, plus regionLookup, producerLookup (9,135), and varietalLookup (31 synonym mappings)
- **Matching is word-boundary based, never raw substring.** Producers named "Cass"/"Pier"/"Rozès" used to match inside "Cassis"/"Pierre"/"Crozes-Hermitage" and misattribute countries. `termMatchesInText` (matchEngine) and `containsTerm` (wineResolver) enforce boundaries — including the possessive rule (Aug 2026): "X's ⟨site⟩" names a vineyard after a person, so `mate` never matches inside "Maté's" (a `(?!'s…)` lookahead; the apostrophe still bounds quoted names). Defense in depth: if a matched producer's country contradicts a country the wine name states outright (name or region term), `resolveWine` caps confidence at 55 — below `PARTIAL_CONFIDENCE_GATE` (60), not merely `CONFIDENCE_GATE` (80) — so a disputed match accumulates NOTHING: the partial band writes region/country points, and producer-derived region/country are exactly the dimensions a geography conflict disputes. The countryAttribution suite + dnaEvolution 10H guard all of this permanently
- Varietal names in wineUnified are combined ("Syrah / Shiraz") — the indexes split them into separately matchable aliases and include varietalLookup synonyms; producer terms index "&"/"et"/"and" conjunction variants
- dnaEvolution.test.js and countryAttribution.test.js inline mirrors of production logic (ESM/JSON-import constraints) — if you change matching logic in matchEngine/wineResolver, update the mirrors; sommPicks.js is dependency-free specifically so its tests hit the real module
- Supabase browser client (supabase.js) FAILS THE BUILD (throws) when `NEXT_PUBLIC_SUPABASE_*` are absent rather than stubbing silently — the static `/` prerender calls `createClient`, so a misconfigured deploy can't go green with dead auth. Server routes use cookie-based sessions via `@supabase/ssr` (`supabaseRoute.js` for cookie-writing redirects, `createServerClient` with no-op cookies for the session-less keepalive)
- **SSRF: any route that fetches a user-supplied URL MUST go through `safeFetch` (ssrfGuard.js)** — never a raw `fetch()` with `redirect: "follow"`. The guard resolves DNS and rejects private/loopback/link-local/metadata addresses on the initial URL and every redirect hop. `/api/fetch-menu` is the current caller
- wineAutocomplete.json (140KB) lazy-loads on quiz Step 5 only — doesn't affect initial load
- `/api/parse-wine-list` accepts base64 image, PDF, or scraped text; returns `{ wines: [...] }` (Path A: structured JSON → match engine) or `{ rawText }` (Path B: text parser). Both converge at `runAnalysis()` in the recommend page
- API GET routes that must run per-request (like `/api/keepalive`) need `export const dynamic = "force-dynamic"` or Next statically prerenders them into no-ops
- **Auth pages read searchParams as a SERVER prop, never `useSearchParams`** — on a statically-served page, useSearchParams suspends during hydration and REMOUNTS the form, wiping typed input. And their submit buttons ship disabled until hydration (`hydrated` state): a pre-hydration click fires a NATIVE form GET submission — full reload, query params dropped, input wiped (and a password-in-URL leak once inputs get `name` attrs). e2e specs wait for the enabled button as their hydration signal before filling
- `ANTHROPIC_API_KEY` is required in Vercel env vars; without it all four Claude routes degrade gracefully (scan errors are friendly, somm and palate-narrative fall back silently). Local `.env.local` carries a key too (added July 29, 2026 — verified live), so Claude paths fully exercise in dev. Cost caveat: every local `npm run test:e2e` run now makes real Claude calls (Vision scans + somm), roughly $0.15–0.30/run

## Priorities (Current)

1. Multi-page scan UX polish
2. Durable rate limiting (Redis/KV) when traffic justifies it — the in-memory limiter (`src/lib/rateLimit.js`) is per-instance; swap the Map for a KV client behind the same `checkRateLimit` signature (no caller changes)
3. Post-launch decisions deferred with Ed: enable Turnstile captcha (`NEXT_PUBLIC_CAPTCHA_ENABLED`, path prepared in `src/lib/captcha.js`); a scheduled weekly auth-funnel health check (the manual ritual is `docs/auth-watchtower.md`)

(The Palate, Act II shipped July 29, 2026. The auth & entry-funnel overhaul — `docs/auth-overhaul-brief.md`, readouts S1–S5 in `docs/` — shipped July 30–31, 2026: the auth core, the never-lose-a-palate seam + anonymous teaser, best-in-class form furniture + magic link, SSRF/rate-limit/build-assertion/observability hardening, and the Session 5 canary fixes: the cross-device pending-palate carry via user_metadata + the confident fallback archetype.)

## API Usage

The Anthropic API is a core part of this product. Three routes, all using `CLAUDE_MODEL` and sharing the rate limiter + cost logging:

- **Wine list scanning** via `/api/parse-wine-list` — measured ~$0.009/scan (text path; images somewhat higher)
- **Bottle label extraction** via `/api/scan-label`
- **Somm curation** via `/api/somm-picks` — measured $0.021–0.025/curation with feedback-rich payloads (3.4–3.6k in / 0.7–0.9k out tokens, 12–20s)
- **Palate narrative refresh** via `/api/palate-narrative` — regenerates the profile narrative only when the palate genuinely moved (a dna_timeline event, or ≥5 newly rated bottles since `narrative_updated_at`); cookie-session auth via `@supabase/ssr` `createServerClient`; ANY failure silently keeps the existing narrative — measured ~$0.006/regeneration (0.8k in / 0.2k out tokens, ~6s)
- Worst-case engaged session ≈ ≤$0.12
- When adding new API-consuming features, be mindful of cost but don't avoid the Anthropic API — it's approved and encouraged

## What NOT to Do

- Don't add new features before existing ones are stable
- Don't introduce new dependencies without a strong reason
- Don't change the brand voice or visual identity without discussion
- Don't optimize prematurely — get it working, then get it fast
- Don't add new paid API integrations without discussion — Anthropic API is the approved exception
- Don't hardcode Claude model IDs anywhere — use `CLAUDE_MODEL` from anthropicConfig.js
- Don't write outcome-tolerant tests for critical paths — at least one spec must hard-fail when the happy path breaks
